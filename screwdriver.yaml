shared:
    image: node:8

jobs:
    main:
        annotations: 
            screwdriver.cd/restrict-pr: fork
        requires:
            - ~pr
            - ~commit
        steps:
            - echo: echo test
